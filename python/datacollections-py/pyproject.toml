[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "datacollections"
version = "0.1.0"
description = "Queryable, self-describing collections of Zarr groups"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
dependencies = [
    "icechunk>=1.0",
    "zarr>=3.0",
    "xarray>=2024.10",
    "numpy>=2.0",
]

[project.optional-dependencies]
# datafusion is pinned, not floated: the published zarr-datafusion-search wheel is
# built against 53, and any other major segfaults across the FFI boundary rather
# than raising. See python/datacollections/query.py.
query = ["pyarrow>=17", "datafusion==53.*", "zarr-datafusion-search>=0.1.2"]
stac = ["stac-fastapi.api>=6", "stac-fastapi.types>=6", "stac-fastapi.extensions>=6", "uvicorn"]
stac-test = ["pystac-client>=0.8", "httpx"]
pandera = ["pandera>=0.31"]
virtual = ["virtualizarr>=2.5", "virtual-tiff>=0.5", "obstore", "arro3-core"]
dev = ["pytest>=8", "jsonschema>=4"]

[tool.maturin]
python-source = "python"
module-name = "datacollections._datacollections"
manifest-path = "Cargo.toml"
features = ["pyo3/extension-module"]

[tool.pytest.ini_options]
testpaths = ["tests"]
